###中断和异常处理程序的嵌套处理

	1.如果嵌套深度大于1的中断是进行进程调度的定时中断，是否会引起直接回到用户态而不是内核态(返回上一层被嵌套的内核控制路径)。
	  如果异常发生时，被定时中断打断进行任务调度。完成进程调度后，中断返回是返回到异常的内核路径还是用户态的进程？
	2.处理缺页异常时，“内核可以挂起当前进程，用另一个进程替代，直到请求的页可以使用为止。只要被挂起的进程又获得处理器，处理
缺页异常的内核控制路径就能恢复”这句话怎么理解。缺页异常时通过什么机制进行的进程切换？
	3.“I/O设备产生的中断并不引用当前进程的专用数据结构”，什么内核路径才会引用当前进程的专用数据结构？流程是怎样的？
	4.既然软中断是稍后处理，为什么在中断处理程序退出时会检查有无激活软中断，并执行软中断处理程序？